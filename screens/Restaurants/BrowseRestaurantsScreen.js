import React from "react";
import { View, Text, Button } from "react-native";

const RestaurantsOverviewScreen = (props) => {
  return <Text>Restaurants</Text>;
};

export default RestaurantsOverviewScreen;
